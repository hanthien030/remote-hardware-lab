# Frontend environment variables

# For local development behind nginx reverse proxy, leaving this as localhost is OK.
# The frontend client already falls back to same-origin when accessed from another LAN device.
VITE_API_URL=http://localhost:5000
